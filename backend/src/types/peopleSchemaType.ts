export interface IPeople extends Document {
  name: string;
  age: number;
  address: string;
  birthOfDate: string;
  deathOfDate: string;
  institution: string;
  causeOfDeath: string;
  history: string;
}
